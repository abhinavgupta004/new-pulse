# Copy to .env for local runs (main.py doesn't auto-load this; export the
# vars or use `python-dotenv`/`set -a; source .env; set +a` before running).
SQLITE_PATH=./news_pulse.db
MAX_FULL_TEXT_PER_RUN=40
FETCH_TIMEOUT_SECONDS=10
CLUSTER_OVERLAP_THRESHOLD=3
KEYWORDS_PER_ARTICLE=12
